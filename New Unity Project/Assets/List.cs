using UnityEngine;
using System.Collections;
using System;

public class ListScript : IComparable<ListScript>
{
    /* Arrays are there to make it so you do not have to write out a whole bunch of similar variables in one script.
     * A list is basically the same thing except that you can do a whole lot more with the variables. What extra you
     * can do with them is a mystery to me but I know that it is possible to do more with Lists. Arrays can be applied
     * to gameObjects as well where it will ask in the interface for a size and the number you put in will represent
     * the number of objects you drag and drop in the list to be referenced.*/
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
